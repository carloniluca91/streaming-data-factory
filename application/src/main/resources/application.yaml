spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: @jdbc.user@
    password: @jdbc.password@
    url: @jdbc.url@

postgres:
  logTable:
    name: @jdbc.log.table@

dataflow:
  webdisp:
    url: http://localhost:8080/api/v1/webdisp